/**
 * 16-way or gate: out = in[0] or in[1] or ... or in[15].
 */

CHIP Or16Way {
    IN in[16];
    OUT out;

    PARTS:
      Or8Way(in=in[0..7], out=OutOr8way1); 
      Or8Way(in=in[8..15], out=OutOr8way2); 
      Or(a=OutOr8way1, b=OutOr8way2, out=out);
}